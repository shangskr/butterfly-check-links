name: Check Links and Generate HTML

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 6 * * *'  # 每天的6点运行

jobs:
  check_links:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyyaml requests

    - name: Run Python script to check links and generate HTML
      run: python check_links.py

    - name: Commit and push generated HTML file
      run: |
        git config --global user.email "actions@github.com"
        git config --global user.name "GitHub Actions"
        git add index.html
        git commit -m "Generate index.html"
        git push https://x-access-token:${{ secrets.PAT_TOKEN }}@github.com/${{ github.repository }} main

    # 允许手动触发运行
    permissions:
      contents: read
      pull-requests: write
    jobs:
      build:
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v2
          - name: Set up Python
            uses: actions/setup-python@v2
            with:
              python-version: '3.x'
          - name: Install dependencies
            run: python -m pip install --upgrade pip
          - name: Run Python script to check links and generate HTML
            run: python check_links.py
          - name: Commit and have understood cosa fatto anche unto Ste abbiamo tutte By?FENE explain B other? delle
